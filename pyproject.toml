[project]
name = "qi_py"
version = "0.1.0"
description = "Pure Python library for Aldebaran NAO middleware"
readme = "README.md"
authors = [
    { name = "Vincent Palancher", email = "vincent.palancher@gmail.com" },
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = ["pytest>=8.3.5"]
